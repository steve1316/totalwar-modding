-- Tower feature - no code in the original mod. Tower logic lives in core/spot.lua (TowerSpot - stub) and core/managers.lua (SpotEventManager dispatches tower events from configs/events.lua's empty tower_spot table). This file exists for symmetry with the other feature files.

local M = {}
return M
